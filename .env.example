# Copy to .env.local and fill in.
# Both variables are optional for local dev — without them the app runs with
# mock data (see src/lib/mockData.ts). Set them to connect to a real Supabase
# project.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
